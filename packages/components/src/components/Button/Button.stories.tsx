import React from 'react'
import { Button } from './Button'

export default {
    title: 'Button'
}

export const Default = () => {
    return (
        <Button>
            button
        </Button>
    )
}